export type Product = {
    id: string
    name: string
    description: string
    price: number
    image_url?: string
}

export type Profile = {
    name: string
    description: string
    username: string
    RUC: string
    whatsapp: string
    address: string
}